alias: Ferme le velux de Lenaïc en cas de pluie
trigger:
  - above: '0.1'
    entity_id: sensor.netatmo_pluie_jardin_rain
    platform: numeric_state
  - entity_id: weather.ti_ar_kaerell
    platform: state
    to: rainy
condition:
  - condition: state
    entity_id: cover.velux_lenaic
    state: open
action:
  - service: script.turn_on
    data:
      entity_id: script.close_velux_lenaic
